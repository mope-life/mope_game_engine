{
  "version": 8,
  "configurePresets": [
    {
      "name": "ninja-multi-config",
      "description": "Use system defaults with the Ninja Multi-Config generator.",
      "generator": "Ninja Multi-Config",
      "binaryDir": "${sourceDir}/build",
      "installDir": "${sourceDir}/install"
    }
  ],
  "buildPresets": [
    {
      "name": "debug",
      "displayName": "Debug",
      "description": "Use system defaults with the Ninja Multi-Config generator. (Debug)",
      "configurePreset": "ninja-multi-config",
      "configuration": "Debug"
    },
    {
      "name": "release",
      "displayName": "Release",
      "description": "Use system defaults with the Ninja Multi-Config generator. (Release)",
      "configurePreset": "ninja-multi-config",
      "configuration": "Release"
    }
  ]
}
